# v2.3.18 — Cleaner upload hint

Patch release: removes the “large PDFs auto-split by page” line from the first-page upload zone. Accepted file types and the 400-file limit are unchanged; large-PDF splitting at runtime is unchanged.

---

## Highlights

- **Upload hint** — dropped auto-split copy from the accepted-files line under the upload zone

---

## UI

### Files

- `public/index.html` — upload-zone accepted-files hint

---

## Deploy / verify

```bash
VERSION="v$(node -p "require('./package.json').version")"
git tag "$VERSION"
git push origin "$VERSION"
```

After deploy, confirm the header shows `v2.3.18` and the upload zone no longer mentions PDF auto-split.

**Full changelog:** https://github.com/dtlb324/Titlework-analyzer/compare/v2.3.17...v2.3.18
