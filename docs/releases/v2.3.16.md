# v2.3.16 — Upload and post-results tips

Patch release: clearer in-app guidance at upload time and after the title opinion is ready.

---

## Highlights

- **Upload tip** — “One recorded instrument per PDF when possible” replaces the background-job close-tab message
- **Post-results tip** — follow-up card reminds users to use **Follow-up** or **+ Add More Documents** to refine ownership without starting over

---

## UI

### Files

- `public/index.html` — upload-zone tip and follow-up card tip

---

## Deploy / verify

```bash
VERSION="v$(node -p "require('./package.json').version")"
git tag "$VERSION"
git push origin "$VERSION"
```

After deploy, confirm the header shows `v2.3.16` and both tips appear on the home upload flow and completed analysis follow-up section.

**Full changelog:** https://github.com/dtlb324/Titlework-analyzer/compare/v2.3.15...v2.3.16
